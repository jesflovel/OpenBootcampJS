//Notación
// ; -> Delimita el dinal de una línea
const b = 4;

b+4;

// . -> Se utiliza en los objetos para acceder a sus atributos
// movil.anchura

// [] -> Se utilizan para listas, arreglos o arrays
const arr = [1,2,3,4];
console.log(arr[2]);

// () -> Se utilizan para funciones
function suma(a, b){
    //codigo de la funcion
}

// {} -> Llaves para objetos, funciones y estructuras de control
const movil = {
    anchura: 5, 
    altura: 10
}

if (true){
    //Todo lo que haya entre llaves


    const constante2 = "Hola";
}